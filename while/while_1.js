var rs = require("readline-sync")
var numero = rs.questionInt("Insira um número: ")
var bip = 1

while (numero > 0) {
    console.log ("BIP BIP")
    var bip = bip + 1
    if (bip > numero)
    break}